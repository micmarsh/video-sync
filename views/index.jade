extends layout

block content
  h1= title

  h2 Upload a video, mothufuckuh!:
  form(action="upload", method="post", enctype="multipart/form-data")
    input(type="file", name="uploadVideo")
    input(type="submit")

